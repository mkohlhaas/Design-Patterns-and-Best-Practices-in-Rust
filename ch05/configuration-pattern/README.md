### The Configuration Pattern

In Rust, the `configuration pattern` typically refers to the Builder Pattern
paired with the Default trait to cleanly initialize and customize complex
structs or application environments. It solves the problem of Rust lacking
named or optional function arguments by using method chaining to modify a
config object step-by-step.


